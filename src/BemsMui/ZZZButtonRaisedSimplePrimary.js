import React from 'react';
import RaisedButton from 'material-ui/RaisedButton';

const ButtonRaisedSimplePrimary = ({ ...props }) => <RaisedButton label="Primary" {...props} primary={true} />;

export default ButtonRaisedSimplePrimary;
