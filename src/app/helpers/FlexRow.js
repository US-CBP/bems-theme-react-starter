import styled from 'styled-components';

const FlexRow = styled.div.attrs({ 'data-sc': 'FlexRow' })`
    width: 100%;
    display: flex;
    flex-direction: row;
    align-items: center;
    position: relative;
`;

export default FlexRow;
